node_modules/
dist/
.env
*.log
qr.png
.DS_Store
*.sqlite*
*.db
data/

# Raw endpoint captures (npm run capture): contain bfWebToken, account ids and
# character names. Scrub by hand before promoting anything to test/fixtures/.
capture/

# Internal AI planning notes — contain local paths / session metadata; never publish
docs/memory/

# Claude Code conversation exports (`/export` writes them here, dated): whole
# sessions, including log excerpts with user ids and egress IPs.
/20??-??-??-*.txt
